// ? string to number

let a = "25";
let b = parseInt(a);
let c = Number(a);

console.log( `hello first number is ${b} and \n second one is ${c}`);

console.log(`Hello, first number is ${b} and \n second one is ${c}`.toUpperCase());

// ? number to string

let d = String(a);

console.log(d)